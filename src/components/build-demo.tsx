import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

const tasks=["ARCHITECTURE","SMART CONTRACT","APPLICATION","TESTS"];
export function BuildDemo(){const [running,setRunning]=useState(false);const [done,setDone]=useState(false);const [value,setValue]=useState("");function generate(){setRunning(true);setDone(false);window.setTimeout(()=>{setRunning(false);setDone(true)},2200)}return <div className="hud-panel p-5 sm:p-8"><label htmlFor="build-prompt" className="font-mono text-xs uppercase text-primary-bright">What do you want to build?</label><div className="mt-4 grid gap-3 sm:grid-cols-[minmax(0,1fr)_auto]"><Input id="build-prompt" value={value} onChange={(e)=>setValue(e.target.value)} placeholder="Build a USDC payment application." className="h-12 rounded-sm border-border bg-background/70 font-mono"/><Button onClick={generate} disabled={running}>{running?"Analyzing intent...":"Generate"}</Button></div><div className="mt-8 grid gap-5">{tasks.map((task,i)=><div key={task}><div className="mb-2 flex justify-between font-mono text-[10px]"><span>{task}</span><span>{running||done?"100%":"0%"}</span></div><div className="h-1.5 bg-muted"><div className={`h-full bg-primary shadow-glow transition-all ${running||done?"w-full":"w-0"}`} style={{transitionDuration:`${700+i*250}ms`}}/></div></div>)}</div>{done&&<p className="mt-8 border border-primary/40 bg-primary/10 p-4 font-mono text-sm text-primary-bright">BUILD READY · SIMULATION COMPLETE</p>}<p className="mt-4 font-mono text-[9px] uppercase text-muted-foreground">Visual demonstration only. No application or contract is deployed.</p></div>}